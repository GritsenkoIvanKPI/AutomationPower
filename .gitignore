# symlink to a shared puppeteer install on the build machine
node_modules

# QA screenshots produced by screenshot.mjs / shot-*.mjs — regenerable, ~328 MB
temporary screenshots/

# macOS
.DS_Store

# rembg virtualenv + rebuild logs (tooling, regenerable)
.venv-bg/
rebuild_progress.log
rebuild_errors.log

# Telegram bot token — must never be committed
config.php
